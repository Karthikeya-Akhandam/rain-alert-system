[project]
name = "rain-alert-system"
version = "0.1.0"
description = "Smart Rain Alert backend and tooling"
readme = "README.md"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
